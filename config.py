# encoding=utf8
import os
import preprocess

script_status_info = 'uncompleted'
raise Exception(script_status_info)

root_path = os.path.dirname(
    os.path.abspath(__file__)
)

tmp_vars = preprocess.Open()
